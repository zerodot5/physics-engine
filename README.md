# physics-engine
a barebones 2d physics engine for my cs class final.

# things to do
X# pause and resume? -> maybe just set dt to 0? ended up just using a boolean to stop the physics function from updating.
X# planet colors based on mass/velocity/random -> random colors
X# add a way to add planets with different masses and velocities -> through terminal
# add gui to change masses and velocities -> make your own with mouse events and shapes
X# add a way to reset the simulation -> button or key
X# add a way to change the gravity coefficient and time step variable
# add gui to change gravity coefficient and time step in real time -> build your own sliders using shapes and mouse events, dont use the slider widgets built in
# zoom and pan?
X# grid background to help see movement
# ability to follow planets in their orbits?
X# if trails not replaced, optimize them to stop lagging with multiple objects -> optimized using batching